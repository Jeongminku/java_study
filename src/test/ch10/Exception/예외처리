• 일반예외(Exception): 컴파일러가 예외처리 코드여부를 검사"하는" 예외를말한다. 
   ㄴ 컴파일러가 try, catch가 있는지 여부를 확인함.


• 실행예외(Runtime Exception): 컴파일러가 예외처리 코드여부를 검사하지 "않는" 예외를말한다




중요포인트

1. 일반예외 vs 실행예외 의 차이.

2. finally는 optional해서 써도되고 안써도 된다.

3. Exception e 는 상위이기때문에 가장 마지막에 써줘야한다 (2개 이상쓸때 기준)

4. throws = 예외를 던져주는것.

5. xxxException을 활용한 사용자 정의 예외 방식.