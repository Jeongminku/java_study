1. 인터페이스란?

- 완전하지 않은 클래스이다.
- 상속을 하기위해 만든 클래스이다.
- 추상메소드와  상수만 가질 수 있다.

ex) add();
    PI=3.14;
    
final을 쓰지않아도 자동으로 상수로 변환된다.

abstract를 쓰지 않아도 자동으로 추상메소드로 변환된다.

interface를 상속하려면 implements를 사용한다.