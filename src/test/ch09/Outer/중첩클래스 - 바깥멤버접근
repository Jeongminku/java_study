바깥 멤버 접근

정적 멤버 클래스 내부에서는 바깥 클래스의 필드와 메소드를 사용할 때 제한이 있음.
	
		구    분		 	 | 바깥 클래스의 사용 가능한 멤버
	인스턴스 		멤버 클래스 : 바깥 클래스의 모든 필드와 메소드
	static(정적) 멤버 클래스 : 바깥 클래스의 static필드 와 static 메소드
	
	
중첩 클래스 내부에서 바깥 클래스의 객체를 얻기위해서는

	바깥클래스이름.this->바깥객체    로 사용할 수 있다.