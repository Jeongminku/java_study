멤버 클래스

멤버 클래스   A $ B .Class (컴파일 파일 이름)

멤버 클래스 > 인스턴스 멤버 클래스 : 선언위치 : class A { class B { ... } }
							객체 생성 조건 : A객체를 생성해야만, B객체를 생성할 수 있음.
							

멤버 클래스 > 정적 멤버 클래스 : 선언위치 : Class A { static Class B {...} }
							객체 생성 조건 : A객체를 생성하지않아도, B객체를 생성할 수 있음.
							
	===========================================================					
							
로컬 클래스
로컬 클래스일 경우 : A $1 B .Class (컴파일 파일 이름)
로컬      클래스 : 선언위치 Class A { void method ( ) { class B {...} } }
				객체 생성 조건 : method 가 실행할 때만 B객체를 생성할 수 있음.
				
	===========================================================	